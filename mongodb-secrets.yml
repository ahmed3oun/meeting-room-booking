apiVersion: v1
kind: Secret
metadata:
  name: mongodb-secret
type: Opaque
data:
  mongo-root-username: cm9vdA==
  # echo -n '<usernameexp>' | base64

  mongo-root-password: cGFzc3dvcmQxMjM=
  # echo -n '<passwordexp>' | base64
